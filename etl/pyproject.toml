[project]
name = "etl"
version = "0.1.0"
description = "Temporal ETL inner project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "pydantic-settings>=2.13.1",
    "temporalio>=1.26.0",
]

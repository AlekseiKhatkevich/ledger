# Beyla v3 configuration for auto-instrumenting Minio HTTP/S3 traffic
otel_traces_export:
  endpoint: http://jaeger:4318
  protocol: http/protobuf
otel_metrics_export:
  endpoint: ""
  interval: 0s
discovery:
  instrument:
    - name: minio
      exe_path: /usr/bin/minio
      open_ports: 9000